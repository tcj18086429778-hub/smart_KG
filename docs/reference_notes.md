# 参考说明

本项目参考了 `neo4j-labs` 项目的三个工程思路：

- 用白名单 schema 限制图谱中的节点和关系类型。
- 把 Neo4j 写入逻辑独立封装，避免业务代码到处拼 Cypher。
- API、数据清洗、规则执行、图数据库写入分层。

没有直接迁移 `neo4j-labs` 的 LLM 文档抽取、Chunk、向量索引和前端代码。原因是本项目第一阶段的数据源是规则表和 GIS 预处理结果，不是非结构化文档。
